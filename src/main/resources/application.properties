spring.application.name=Home

# æ°æ®åºéç½®
spring.datasource.url=jdbc:mysql://localhost:3306/homebook?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=123456
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPAéç½®
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# Let Hibernate auto-detect the dialect from JDBC metadata
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# è¿æ¥æ± éç½®
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=20000

server.port=8081

# Enable Flyway migrations. If your DB already contains schema, baseline on migrate.
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true

# Make JSON use snake_case to match frontend adapter expectations
spring.jackson.property-naming-strategy=SNAKE_CASE

# Mail settings for QQ SMTP (fill with actual credentials or environment variables)
spring.mail.host=smtp.qq.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:1022229666@qq.com}
spring.mail.password=${MAIL_PASSWORD:bvhxlyhjhbrvbeig}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true